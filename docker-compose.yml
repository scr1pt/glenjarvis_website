db:
  image: postgres
  ports:
    - "5432"
web:
  build: .
  # command: bundle exec rails s -p 3000 -b '0.0.0.0'
  # volumes:
    # - .:/usr/src/app
  ports:
    - "3000:3000"
  links:
    - db
proxy:
  image: nginx
  # image: nginx-proxy  # needs DNS setup and VIRTUAL_HOST